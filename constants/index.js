const { MESSAGES } = require('./messages');
const { RESPONSE_STATUS } = require('./response-status');
const { API_URLS } = require('./api-urls');

module.exports = {
  MESSAGES, RESPONSE_STATUS, API_URLS
};
